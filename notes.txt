two different classnames for a single element

mask-image: linear-gradient(to bottom, rgba(0, 0, 0, 1), rgba(0, 0, 0, 0));

{Array(rating).fill().map((_, i) => (
                        <p>⭐</p>
                    ))}
